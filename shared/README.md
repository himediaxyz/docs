# shared

여러 문서 템플릿이 공통으로 쓸 조각을 모아두는 폴더입니다. 지금은 템플릿이 `templates/gongmun/` 하나뿐이라 비어 있지만, 두 번째 문서 종류를 만들 때 아래와 같은 것들을 여기로 분리하는 것을 권장합니다.

- 브랜드 색상·폰트 CSS 변수 (`--navy`, `--font-kr`, `--font-en` 등)
- 헤더 밴드 / 로고 배치 마크업
- 푸터(주소·연락처) 마크업
- 발신인 서명 블록 마크업
- 공통 인쇄(`@page`) 및 print-color-adjust 규칙
