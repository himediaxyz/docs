/* ============================================================
   recipient-info.js
   자주 쓰는 "수신인(받는 사람)" 후보 목록 — 문서 상단 도구모음의
   수신인 드롭다운(shared/scripts/editor.js 섹션 15)이 이 목록을 읽어
   보여주고, 고르면 문서의 수신 영역(회사 이름·담당자 이름·주소·
   이메일)을 한 번에 채워 넣습니다.

   sender-info.js(발신인 목록)와 짝을 이루는 파일이지만 성격은 다릅니다:
   발신인은 "우리 회사 사람"이라 몇 명으로 정해져 있지만, 수신인은
   "문서를 보내는 상대"라 매번 다를 수 있습니다. 그래서 드롭다운
   맨 위에는 이 목록에 없는 상대를 그때그때 직접 입력할 수 있는
   "새로 입력" 항목이 항상 따로 있고(고르면 수신 영역이 빈 안내
   문구로 초기화되어 바로 타이핑할 수 있습니다 — editor.js의
   resetRecipientFields() 참고), 이 목록은 어디까지나 "자주 보내는
   상대라 미리 등록해두면 편한" 선택 사항입니다.

   각 항목은 다음 네 가지를 모두 선택 항목으로 둡니다:
     companyName  회사 이름
     contactName  담당자 이름
     address      회사 주소
     email        이메일 주소
   요청사항대로 수신인은 "회사만/회사+담당자/담당자만"일 수 있고
   주소·이메일도 있을 때만 채우면 됩니다 — 없는 항목은 아예 키를
   빼거나 빈 문자열로 두세요. 드롭다운에서 고르면 그 조합에 맞춰
   문서 쪽 수신 영역이 자동으로 정리됩니다(예: 주소가 없으면 주소
   줄 자체가 감춰짐 — editor.js의 applyRecipient() 참고).

   id: 드롭다운에서 어떤 항목을 선택했는지 구분하는 값 — 같은 회사
   안에서만 겹치지 않으면 됩니다.

   지금은 이 배열을 직접 손으로 고쳐서 추가/수정/삭제합니다(이
   사이트는 서버/DB가 없는 순수 정적 사이트라 sender-info.js와 같은
   방식입니다). 아래 두 항목은 조합을 보여주는 예시이니, 실제 자주
   보내는 수신처로 자유롭게 바꾸거나 지우고 필요한 만큼 추가해서
   쓰세요.

   이 파일을 쓰는 곳: 사용자가 직접 입력하는 문서 템플릿
   (company-info.js, sender-info.js 다음, editor.js보다는 먼저
   불러오면 됩니다)
     <script src="../../shared/scripts/recipient-info.js"></script>
   ============================================================ */

window.DISE = window.DISE || {};

DISE.recipients = {

  disehimedia: [
    {
      // 예시 1: 회사 + 담당자 + 주소 + 이메일 — 네 가지가 다 있는 경우.
      id: 'example-company-contact',
      companyName: '예시 주식회사',
      contactName: '홍길동 팀장',
      address: '서울특별시 강남구 테헤란로 123, 4층',
      email: 'contact@example.com'
    },
    {
      // 예시 2: 회사 이름만 있는 경우(담당자·주소·이메일 없음) — 문서
      // 수신 영역에는 회사 이름만 나오고 나머지 줄은 자동으로
      // 감춰집니다.
      id: 'example-company-only',
      companyName: '예시 물류 주식회사'
    }
    // 담당자만 있고 회사가 없는 경우도 같은 형식으로 추가할 수
    // 있습니다(companyName 없이 contactName만 넣으면 됩니다).
  ]

  // r2v / bic / axis-one: 수신처가 정해지면 disehimedia와 동일한
  // 형식으로 키를 추가
};
